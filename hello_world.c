// We must include this line to have access to the printf method
#include <stdio.h>

// Every C program begins in a function called main()
// Inside main(), we can call other functions or execute some code
// "int" tells us that main() will return an integer
int main() {
  printf("Hello World!");
  return 0;
}

// Let's declare some variables
// Define the data type and then the name of the variable, remembering to end the expression
// with the ;
int my_number;
char a, b, c, d;
float the_float;
